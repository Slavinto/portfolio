export interface ProjectTabsConstants {
    title: string;
    value: string;
    imageName: string;
    hasTheme?: boolean;
}

export interface LoginCredentials {
    email: string;
    password: string;
}
