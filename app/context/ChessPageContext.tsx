// import { initialBoardState } from "@/data/games/chess/constants/initialBoardState";
// import { createContext, FC, PropsWithChildren, useContext } from "react";

// export const ChessPageContext = createContext({ state: initialBoardState });

// export const useChessPageContext = () => useContext(ChessPageContext);

// export const ChessPageContextProvider: FC<PropsWithChildren> = ({
//     children,
// }) => {
//     return (
//         <ChessPageContext.Provider value={}>
//             {children}
//         </ChessPageContext.Provider>
//     );
// };
